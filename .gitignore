# Local maintenance skills (live in ~/.claude/skills/)
skills/

# Dependencies
node_modules/

# Build output
dist/
.astro/

# Skill telemetry (personal session data)
telemetry/skill-log.jsonl

# Claude Code local config
.claude/

# NOTE: content/, learnings/, AUDIT.md are tracked for cross-machine sync.
# Keep them private by never making this repo public without a re-audit.
